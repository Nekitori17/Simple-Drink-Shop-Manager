{
  "name": "simple-drink-shop-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm --filter frontend build && pnpm --filter backend build",
    "build:vercel": "pnpm --filter frontend build && pnpm --filter backend build:vercel",
    "vercel-build": "pnpm run build:vercel && rm -rf .vercel/output && mkdir -p .vercel && mv apps/BackEnd/.vercel/output .vercel/output",
    "deploy": "vercel --prod",
    "clean": "pnpm -r exec rm -rf dist .output .vercel",
    "db:generate": "pnpm --filter backend exec drizzle-kit generate",
    "db:push": "pnpm --filter backend exec drizzle-kit push",
    "db:studio": "pnpm --filter backend exec drizzle-kit studio"
  },
  "keywords": [],
  "author": "Nekitori17",
  "license": "MIT",
  "packageManager": "pnpm@10.29.3"
}
